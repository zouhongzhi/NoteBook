<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle" 
    >
    
    <!-- 实心，就是填充的意思 android:color指定填充的颜色 -->
    <solid 
        android:color="@color/white" 
        />
    
    <!--渐变 android:startColor和android:endColor分别为起始和结束颜色，
    android:angle是渐变角度，必须为45的整数倍。当angle=0时，渐变色是从左向 右。 
            然后逆时针方向转，当angle=90时为从下往上。另外渐变默认的模式为android:type="linear"，
            即线性渐变，可以指定渐变为径向渐变， android:type="radial"，
            径向渐变需要指定半径android:gradientRadius="50"，也可以指定二者的综合，
            扫描渐变 android: type="sweep"-->
    <!-- <gradient
        /> -->
    
    <!-- 描边 android:width="2dp" 描边的宽度，android:color 描边的颜色。 
            我们还可以把描边弄成虚线的形式，设置方式为： android:dashWidth="5dp" 
    android:dashGap="3dp" 其中android:dashWidth表示'-'这样一个横线的宽度，
    android:dashGap表示之间隔开的距离 -->
    <stroke
        android:width="@dimen/dimen_small"
        android:color="@color/main_edittext_frame_color" 
        />
    
    <!-- 内边距 -->
    <padding
        android:bottom="@dimen/dimen_small"
        android:left="@dimen/dimen_small"
        android:right="@dimen/dimen_small"
        android:top="@dimen/dimen_small" 
        />
    
    <!-- 圆角 android:radius为角的弧度，值越大角越圆。 我们还可以把四个角设定成不同的角度，
    	方法为： android:topRightRadius="20dp" 右上角android:bottomLeftRadius="20dp" 
    	右下角android:topLeftRadius="1dp" 左上角android:bottomRightRadius="0dp" 
    	左下角 这里有个地方需要注意，bottomLeftRadius是右下角，而不是左下角 -->
    <corners
        android:radius="@dimen/dimen_medium" 
        />
</shape>